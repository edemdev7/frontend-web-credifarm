export interface IEntry {
    id: string;
    name: string;
    description: string,
    price: number,
    category: string,
    creationDate: string,
    image: string,
  }
  

export interface INewEntry {
    name: string;
    description: string,
    price: number,
    category: string,
    image: string,
}
  