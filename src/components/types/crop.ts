export interface ICrop {
    id: string;
    name: string;
    description: string,
    category: string,
    creationDate: string,
    image: string,
  }
  

export interface INewCrop {
    name: string;
    description: string,
    category: string,
    image: string,
}
